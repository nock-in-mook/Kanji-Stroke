# 引き継ぎメモ 2026-03-02（セッション12）

## 今回やったこと

### Bezier曲線の密サンプリング
- `_cubic_bezier_sample()` 新設: 3次Bezier曲線を10分割サンプリング
- `parse_svg_path` 全面改修: C/c/S/sコマンドで曲線上の中間点も記録
- S/sコマンドの前cp2反転処理も正しく実装

### ウェイポイント疎・ガイドポイント密の分離
- ウェイポイント（グラフノードマッチング用）: 最大8点（疎）
- ガイドポイント（エッジペナルティ計算用）: Bezier全点（密）
- `parse_kvg_strokes` が `all_points` フィールドも返すように拡張

### 使用済みエッジペナルティ
- `used_edges` 辞書で各ストロークの使用エッジを追跡
- `USED_EDGE_PENALTY=50.0` でグラフレベルの重複回避
- `used_pixels` setで `USED_PIXEL_COST=3.0` 倍のピクセルレベル重複回避
- `record_used_edges()` で経路のピクセル→エッジの逆引き

### 迂回検出閾値引き下げ
- 3倍閾値（4倍→3倍）でピクセルレベル経路への切り替えを早期化

### 品質結果
- 80/80 成功
- A: 37字, B: 23字, C: 20字（前回 A:30, B:17, C:33）
- C評価が33→20に大幅削減（39%改善）

### 変更ファイル
- `auto_strokes.py` — 大改修
- `DEVLOG.md` — セッション10-12の記録追加
- `HANDOFF.md` — このファイル

## 次にやること

### 残課題
- **C評価20文字の改善**: 主に複雑漢字（学・校・森・車・音・雨など）
  - 学: 冖部分のルーティングが複雑
  - 森: 12画で木3つの重なりが多い
  - 車: ㇆フレームストローク
  - 音: 上部の立+日が密集
- **短い点画（㇔）**: 骨格上で3-4pxしかなく正確な表現が困難
- **テーパー描画の復活**: データはあるので、描画ロジックを改善して再有効化
- **デプロイ**: 修正後のstroke_dataをCloudflare Pagesにデプロイ

### 品質改善の方向性
1. **ストローク間のKVG方向ベクトル整合性チェック**: ウェイポイントが始点→終点の大まかな方向に沿っていない場合はスキップ
2. **㇆/㇕特化処理**: フレームストロークを角ポイントで2セグメントに分割
3. **used_edgesの方向考慮**: 同方向に通るエッジはペナルティ軽減（共有が正しい場合もある）

### ファイル構成
```
Kanji_Stroke/
├── index.html              # アニメーションビューア
├── editor.html             # 書き順エディタ v2（テーパー描画一旦無効）
├── auto_strokes.py         # 骨格グラフ方式のストローク自動生成（Bezier+使用済みペナルティ）
├── visualize_strokes.py    # 可視化ツール
├── stroke_data/            # 80文字のJSONデータ
├── viz_output/             # 可視化画像（900x900 PNG × 80枚）
├── fonts/KleeOne-SemiBold.ttf
├── CLAUDE.md / ROADMAP.md / DEVLOG.md / HANDOFF.md
```
