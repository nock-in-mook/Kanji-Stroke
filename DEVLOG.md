# DEVLOG - 漢字書き順ジェネレーター

---

## 2026-03-02（セッション19-20）

### ノードマッチング改善 + コーナータイプ拡張

自律改善ループ継続。ノードマッチング精度向上とコーナータイプストロークの直角化を拡張。

**セッション19:**
1. `used_end_nodes`: 使用済み終点ノード回避（年の三角問題解消）
2. `蓄積Yオフセット`: 前ストロークのKVG-骨格Yオフセットを後続に適用（下がC→A）
3. `step 6j`: ㇔長さ制限（KVG距離×1.5超過時に切り詰め）

**セッション20:**
4. `㇄→㇕/㇆同等化`: step 6b/6g/候補5に㇄追加（出の㇄が直角に）
5. `㇖→直角強制対象`: step 6gに㇖追加
6. `step 6k`: コーナータイプ2点ストロークの角点復元
   - ノード位置から幾何学的にL字角点挿入（H→V: corner=(end.x,start.y), V→H: corner=(start.x,end.y))
   - 子/字/学/空の㇖ストロークが直線→L字に改善

**品質推移:**
- セッション19開始: A:20, B:27, C:28, D:5
- セッション20終了: A:~22, B+:~16, B:~25, B-:~3, C:~6, D:~2
- C/D: 33字→8字（-76%）

---

## 2026-03-02（セッション17-18）

### 後処理パイプライン大強化（step 6c-6h）

自律改善ループ継続。6つの後処理ステップを追加。

**セッション17 (step 6c-6f):**
1. `step 6c`: 純㇐/㇑の強制直線化（3点以上→2点に圧縮、angle_ratio<0.4）
2. `step 6d`: ジグザグ除去（両隣>45°逸脱 + <25pxの短セグメント→除去）
3. `step 6e`: 波打ち除去（dx/dy符号交互反転→中間点除去）
4. `step 6f`: 始点/終点フック除去（全体方向>45°逸脱 + <25px→除去）

**セッション18 (step 6g-6h):**
5. `step 6g`: ㇕/㇆直角強制 — H/V遷移ベースの角点検出で台形→矩形に（28件発火）
   - 末尾フック誤検出回避のためグローバルH/V遷移判定を採用
   - corner = (corner.x, start.y) でH→V、(start.x, corner.y) でV→H
6. `step 6h`: ㇑/㇐軸揃え — 2点ストロークの微小ズレ修正（194件発火）
   - ㇑: x平均化で完全垂直、㇐: y平均化で完全水平

**結果:** A:14字, B:35字, C:31字
- 口/田/日/目/土/王がA昇格（㇕直角化+軸揃えが決め手）
- 百/円/雨の台形㇆→矩形化
- 本のはらいジグザグ解消、手の波打ち解消、百の三角フック除去

**後処理パイプライン全体:**
```
5. RDP簡略化 → 6. fit_line_if_straight → 6b. ㇕/㇆セグメント直線化
→ 6c. 純㇐/㇑強制直線化 → 6d. ジグザグ除去 → 6e. 波打ち除去
→ 6f. フック除去 → 6g. ㇕/㇆直角強制 → 6h. ㇑/㇐軸揃え → 7. clip_to_outline
```

---

## 2026-03-02（セッション16）

### KVG骨格スナップ強化・セグメント別直線化

自律改善ループ継続。5つの改善を実装。

**主な改善:**
1. `kvg_skeleton_snap_path`: KVGポイントを骨格に直接スナップ（Dijkstra不要）
2. `find_corner_point`: グローバル方向変化での角検出（㇕/㇆の角を確実に検出）
3. セグメント別直線化: ㇕/㇆を角点で分割し各辺を独立直線化
4. 段階的H/V直線化閾値: 角度比<0.15で3x、<0.35で2x
5. 検索半径拡大: KVGスナップの search_radius 15→30フォールバック

**結果:** A:13字, B:51字, C:16字（前セッション C:~20字から改善）
- 一: 半分→フル幅、目: V字→直線、車: 7点ジグザグ→3点直角

---

## 2026-03-02（セッション13）

### font_bmpフォールバック + KVG偏差チェック + ガイド強化

セッション12からの継続改善。3つの新メカニズムを実装。

#### 改善内容
1. **font_bmpフォールバック**: 骨格が届かない短い㇔点画でskeleton_pixel_pathがfont_bmpピクセルをフォールバック
   - `_find_nearest_font_pixel()` 新設
   - 始点=終点ノード時の即座ピクセルパス切替
   - 連結成分チェック: font_bmpフォールバック時はスキップ
   - **KVGフォールバック0件を達成**（学の画2・3など全解消）

2. **KVG偏差チェック**: グラフ経路のKVGからの平均偏差を計算し、15px超ならピクセル経路に切替
   - 「長さは合ってるが間違ったルートを通っている」ケースを捕捉
   - 四の㇕フレームなどが劇的改善

3. **ガイド強化**:
   - グラフDijkstraのpenalty_weight: 3.0 → 5.0
   - KVG偏差閾値: 20px → 15px
   - 迂回検出閾値: 3倍 → 2.5倍
   - 短いストローク（KVG距離40px未満）はピクセルパスと比較し最適を選択

#### 品質結果（目視評価）
- 80/80文字成功（KVGフォールバック0件）
- 立・本・水・耳・空・四などが大幅改善
- 残課題: 校・森・糸・年・手など密集部の複雑漢字

#### 変更ファイル
- `auto_strokes.py` — font_bmpフォールバック、KVG偏差チェック、ガイド強化

---

## 2026-03-02（セッション12）

### Bezier曲線サンプリング + 使用済みエッジペナルティ

前セッション(10-11)で導入した4つの新メカニズムに加え、さらに2つの改善を実施。

#### 1. Bezier曲線の密サンプリング（parse_svg_path改修）
- **問題**: KVGのSVGパスにはC/c/S/sコマンド（3次Bezier曲線）が多用されているが、従来は終点のみ記録し曲線情報を破棄していた
- **修正**: `_cubic_bezier_sample()` を新設。各Bezier曲線を10分割でサンプリング
- **S/sコマンド**: 前のcp2の反転でcp1を自動計算する処理も追加
- **効果**: ガイドポイントが格段に密になり、曲線ストロークの精度向上

#### 2. ウェイポイント疎・ガイドポイント密の分離
- **問題**: Bezierサンプリングで点数が増えすぎると、ウェイポイントが誤った骨格枝にマッチして迂回が発生
- **修正**: ウェイポイント（グラフノードマッチング用）は最大8点、ガイドポイント（エッジペナルティ計算用）はBezier全点を使用
- **効果**: 気の4画目 843→310px、男の6画目 557→255px

#### 3. 使用済みエッジペナルティ
- **問題**: 前のストロークと同じ骨格エッジを通る（特に田・口・目型の囲み構造）
- **修正**: `used_edges` 辞書でストロークごとの使用エッジを追跡、`USED_EDGE_PENALTY=50.0` を加算
- **ピクセルレベル**: `used_pixels` setで使用済みピクセルにも `USED_PIXEL_COST=3.0` 倍のコスト

#### 4. 迂回検出閾値の引き下げ
- **変更**: 経路がKVG直線距離の4倍→3倍を超えたらピクセルレベルに切替
- **効果**: 気・男・校など多くの複雑漢字で迂回を早期検出

### 検証結果（全80文字）
- 80/80 成功（失敗0）
- A(良好): 37字 ← 前回30字
- B(概ね良好): 23字 ← 前回17字
- C(要改善): 20字 ← 前回33字
- **C評価が33→20に大幅削減（39%改善）**

### 変更ファイル
- `auto_strokes.py` — `_cubic_bezier_sample()` 新設、`parse_svg_path` 全面改修、`shortest_path_directed` に used_edges パラメータ追加、`skeleton_pixel_path` に used_pixels パラメータ追加、`generate_strokes()` に使用済みエッジ追跡を組み込み

---

## 2026-03-02（セッション10-11）

### 骨格グラフのルーティング大改善（4つの新メカニズム）

1. **方向考慮Dijkstra** (`shortest_path_directed`) — KVGパスからの偏差をエッジコストにペナルティ加算（`penalty_weight=3.0`）
2. **骨格ギャップ修復** (`bridge_skeleton_gaps`) — Zhang-Suen後の切断コンポーネントを`max_gap=35`で自動ブリッジ
3. **ピクセルレベルDijkstra** (`skeleton_pixel_path`) — グラフ切断時に骨格ピクセルを直接通る経路探索
4. **経路長バリデーション** — KVG直線距離の4倍以上迂回する経路をピクセルレベルで再試行

### ウェイポイント密度の16分割化
- 従来: KVGパスの中間点を8分割（7点）
- 改善: 16分割（15点）でガイド精度向上

### 1年生漢字80字の全生成
- 80/80文字すべて成功（0失敗）
- 品質: A(良好)30字 + B(概ね良好)17字 + C(要改善)33字

---

## 2026-03-02（セッション9）

### 骨格グラフ改善（auto_strokes.py）

スクショフィードバックから5つの問題を特定し、自律的に修正→可視化→検証のループを実施。

#### 1. 交点を超えた延長（branch → end node）
- **問題**: KVG終点がbranchノード（交差点）にマッチし、その先のend nodeまで延長されない
- **対象**: 山3画目、日1画目、口1画目、田1画目、中1画目
- **修正**: `find_extension_end_node()` を新設。経路の末端がbranchの場合、KVG方向に沿ったend nodeを探して延長
- **安全弁**: KVG端点に近づく方向のみ延長（木の左はらいが横棒端に誤延長される問題を修正）

#### 2. 口の2画目（㇕）ルート修正
- **問題**: shortest_pathが左辺→下辺を選んでいた（正解: 上辺→右辺）
- **修正**: KVGパスの中間点サンプリングを4分割→8分割に増やし、waypointの精度向上
- **結果**: 口・日・田の㇕がすべて正しいルート（上辺→右辺）を通るようになった

#### 3. ジグザグ検出・除去
- **問題**: 経路が交差点付近で余計ピクセルを往復する
- **修正**: `remove_zigzags()` を新設。RDP後に連続3点の折り返し角度を検出、短い折り返しを除去
- **条件**: cos < -0.5（120度以上の折り返し）かつ辺長15px未満

#### 4. 始点の「クィッ」曲がりトリミング
- **問題**: フォント装飾（セリフ）が骨格に残り、始点/終点でクィッと曲がる
- **修正**: `trim_serif()` を新設。経路の先頭/末尾8%をトリミングし、残りの方向に直線延長
- **適用条件**: 経路が10ピクセル以上の場合のみ

#### 5. テーパー描画の一旦無効化
- **editor.html**: 全ストロークを均一幅（stroke-width:10）で描画するように変更
- テーパーデータ（taper: true/false）は保持。描画のみ無効化

### 検証結果（全14文字）
- ✅ 一、中、九、人、力、口、大、山、川、日、木、水、田 — 良好
- ⚠️ 火 — 1・2画目の「点」が短い（骨格構造上の制約）
- 主要な5問題はすべて改善

---

## 2026-03-01（セッション7）

### リモート接続時のチャット履歴復元

#### 問題
- `/remote-control` は新しいセッション（新しい.jsonlファイル）を作る
- スマホから接続すると、PC側の会話履歴が見えない
- AI側も前のセッションの文脈を持たない

#### 解決: transcript_export.py
- `.jsonl` → 読みやすいMarkdown (`SESSION_LOG.md`) に自動変換
- SessionStartフック経由で `/remote-control` 実行時に自動生成
- スマホ側で「SESSION_LOG.md読んで」と指示するだけでコンテキスト復元

#### 技術的な課題と解決
- **メッセージ重複**: `user` と `queue-operation` が同じ内容を記録 → `seen_user_texts` setで重複排除
- **システムタグ混入**: `<system-reminder>`, `<task-notification>` 等 → 正規表現で除去
- **セッション断裂**: `/remote-control` で新jsonlが作られる → 60秒以内の連続セッションを自動マージ
- **cp932エラー**: Windows のデフォルトエンコーディング → `encoding='utf-8'` を明示

#### 出力フォーマット
```
【あなた】
ユーザーの発言

【Claude】
実質的な回答

↓
(作業)
↓

【Claude】
作業結果の報告
```

### /remote コマンドの廃止
- カスタム `/remote` を完全削除、`/remote-control`（ビルトイン）に統合
- Slack通知 + 履歴変換をすべて SessionStart フック内で処理

---

## 2026-03-01（セッション5）

### スナッププレビューの実装
- **問題**: タップするまでスナップ先がわからず、配置後にズレる
- **解決**: スナップカーソル（オレンジのクロスヘア + 中心ドット）を追加
  - pointerdown/pointermove でスナップ先をリアルタイム表示
  - pointerup では `lastSnappedPos` を使い、プレビュー位置に正確に配置

### 点マーカーの改良
- **問題**: 塗りつぶしの丸だと、点の中心位置が見えない
- **解決**: 中抜き円 + 中心ドット(r=2) + 番号を左上に小さく表示
  - 中心がどこにあるか正確に視認できるようになった

### 中心線アルゴリズムの進化

#### v1: 走査線方式（不採用）
- 水平・垂直に3px間隔で走査 → 断面中点をストローク別にグループ化
- **問題**: 斜め・曲線の画では中心がアウトラインの外にはみ出す
- 原因: 走査方向がストロークに対して斜めなので、断面中点≠真の中心

#### v1.5: 走査線 + pointInPolygon フィルタ（改善不十分）
- アウトライン外の中心点を除外 → はみ出しは減ったが根本解決にならず
- 水平・垂直の両方が同じ斜め画を検出 → 2本の平行線が出る問題も発生
- マージ・平均化を試みるも効果限定的

#### v2: Zhang-Suen 細線化 + ポリゴンラスタライズ（改善不十分）
- アウトライン多角形をビットマップに描画 → Zhang-Suen 細線化 → 骨格抽出
- 交差部分は改善されたが、ポリゴンが39点の直線近似のため曲線部分でズレが残る

#### v3: Zhang-Suen 細線化 + Canvas フォント描画（採用）
- **根本解決**: アウトラインポリゴンではなく、Canvas に実際のフォントを描画
- `ctx.fillText()` でKlee Oneを300x300のCanvasに描画 → ピクセルデータ取得
- Zhang-Suen 細線化で1ピクセル幅の骨格に → ポリラインとして表示
- **メリット**:
  - フォントの実際の曲線形状に完全一致
  - GLYPH_DATA 不要 → 任意の漢字で中心線が動作
  - アウトライン内に必ず収まる（骨格は内部ピクセルのみ）

### Zhang-Suen 細線化アルゴリズムの仕組み
```
1. フォントをCanvasに描画 → alpha > 128 のピクセルを前景(1)とする
2. 境界ピクセルを条件付きで削除（形の連結性を壊さない）
   - 8近傍の黒ピクセル数が2〜6（孤立点や完全に囲まれた点は除外）
   - 0→1遷移が1回だけ（連結性を保証）
   - Step1/Step2で異なる方向条件（北西/南東の交互削り）
3. 削除できるピクセルがなくなるまで繰り返し → 1px幅の骨格が残る
4. 骨格ピクセルを端点から順にたどってポリラインに変換
```

### 現状
- エディタの操作感が大幅に改善
- 中心線スナップが全漢字で動作
- Canvasベースライン: y=259（CSS flexbox center + Klee One メトリクスに合わせて計算）

---

## 2026-02-28（セッション4）

### フリーハンド描画の限界と方式転換

#### フリーハンド描画の問題（Plan A検証）
- 「九」でフリーハンド描画をテスト → カクカク（Douglas-Peucker EPSILON 3.0→1.5 に調整しても改善不足）
- **根本的な問題**: 始点と終点が定まらないため書き直し回数が尋常じゃなくなる
- ユーザー判断: フリーハンド方式は不採用

#### 点配置方式への移行（Plan B）
- **方式**: タップで点を配置 → Catmull-Rom スプラインで滑らかな曲線を自動生成
- editor.html を完全書き換え
- Catmull-Rom → 3次ベジェ変換: `CP1 = P1 + (P2-P0)/6`, `CP2 = P2 - (P3-P1)/6`
- 2点 = 直線、3点以上 = 滑らかな曲線が自動生成
- リアルタイムプレビュー（半透明）で配置中に確認可能

### タッチ操作の改善
- **問題1**: 2本指ピンチズームで線が描かれてしまう
  - `touch-action: none` → `pinch-zoom` → `manipulation` に変更
  - マルチタッチ検出で描画キャンセル
- **問題2**: キャンバス上でスクロールできない
  - `touch-action: manipulation` + touchstart/touchmove で1本指のみ preventDefault
  - 結果: 1本指=点配置、2本指=スクロール＆ピンチズーム

### 「九」のグリフデータ追加
- `extract_points.py 九` で座標抽出
- GLYPH_DATA に九のアウトライン（39点）を追加
- centerLines は直線のみで曲線ストロークに対応不可 → 手動 centerLines を廃止

### 点のドラッグ移動
- 配置済みの点を20px以内でタッチ → ドラッグ開始
- ドラッグ中は点が大きくなり色反転（視覚フィードバック）
- 40px Y方向オフセット（指の上に浮く）
- ドラッグ中もリアルタイムに曲線プレビュー更新

### 中心線スナップの実装

#### v1: レイキャスト方式（不採用）
- タップ位置から水平・垂直レイキャスト → アウトライン交点ペアの中点
- **問題**: フチに引っ張られる（交点ペアの選択が不安定）

#### v2: 走査線方式（採用）
- アウトラインを3px間隔で水平走査 → 各断面中点を計算 → 縦画の中心線
- 同様に垂直走査 → 横画の中心線
- 隣接走査線の中点を近傍マッチング（15px以内）で連結 → ストローク別ポリライン
- 断面幅 50px 以下のみ対象（広い領域はスキップ）
- **表示**: オレンジ点線（stroke-dasharray: 4,4, opacity: 0.6）
- **スナップ**: 全中心点から25px以内の最近傍にスナップ
- チェックボックスで ON/OFF 切替（デフォルト ON）
- 漢字入力変更時にキャッシュ自動無効化＆再計算

### 現状
- エディタ v2 は点配置方式で実用レベル
- 山・九の2文字にアウトラインデータあり
- 中心線の走査線検出は縦画・横画とも正常動作
- はね・はらいの終端表現は未実装

---

## 2026-02-28（セッション2〜3）

### フォント統一問題の解決
- **問題**: extract_points.py が NotoSansJP（ゴシック体）で抽出した座標を、表示は Noto Serif JP（明朝体）で重ねていた → 字形が違うのでズレる
- **調査**: 抽出にも表示にも使えるフォントを比較検討
  - Klee One SemiBold（硬筆体） ← 採用
  - BIZ UD Mincho、Noto Serif JP、Zen Old Mincho も比較
- **font_compare.html** を作成して4フォント×8漢字を比較、Cloudflare Pages にデプロイしてスマホで確認
- **決定**: Klee One SemiBold — 硬筆体でとめ・はね・はらいがはっきり、SIL OFL 1.1で安全

### フォント導入
- `fonts/KleeOne-SemiBold.ttf`（8.5MB）をプロジェクトにダウンロード
- editor.html / index.html を `@font-face` でセルフホスティングに変更
- extract_points.py のフォントパスも変更

### 座標系の修正
- **問題**: Python 抽出の座標とブラウザ表示の座標がスケール・位置ともにズレる
- **原因**: extract_points.py がバウンディングボックスフィットで計算していたが、ブラウザはCSS font-size / flexbox center / line-height:1 の計算で描画
- **修正**: extract_points.py を CSS と同じ座標変換に書き直し
  - `scale = font_size / units_per_em`（250/1000 = 0.25）
  - baseline_y = line_box_top + half_leading + ascent * scale
  - Y軸反転: `ny = baseline_y - y * scale`
- Klee One メトリクス: unitsPerEm=1000, ascent=1160, descent=-288, advanceWidth=1000
- 修正後、アウトライン座標がフォント表示とぴったり一致

### GLYPH_DATA 形式の変更
- 旧: `contours[]`（4つの矩形 = Noto Sans JP の構造）
- 新: `outline[]`（1つの連続多角形 = Klee One の構造）+ `centerLines[]`（各画の中心線）

### エディタ調整モードの実装

#### v1: 全点ドラッグ
- outline（灰色）と centerLine（色付き）の全点を表示
- 色付き点のみドラッグ可能に

#### v2: 指オフセット
- ドラッグ中の点が指の下に隠れる問題
- SVG座標で40px上にオフセットして描画（指の上に浮く）

#### v3: 線選択方式
- 点が重なっている箇所で狙った点をタップできない問題
- 解決: 先に線をタップして選択 → 選択した線の端点のみ大きく表示＆ドラッグ可能
- `distToSegment()` で線分への最短距離を計算、30px以内でタップ判定
- 未選択の線は薄く表示（opacity: 0.3）

#### v4: スナップ機能
- **点マージ**: ドラッグ中の端点が他の端点に15px以内に接近 → 同じ座標にスナップ
- **線スナップ**: ドラッグ中の端点が他の中心線に15px以内に接近 → 線上の最近点にスナップ
- 両方ともチェックボックスで ON/OFF 切り替え可能（デフォルトON）
- 調整モード中のみスナップ設定が表示される

### 現状
- editor.html のエディタ機能は実用レベルに到達
- 「山」の centerLines データはまだ手動設定のみ
- はね・はらいの終端表現は未実装

### 次回やること
- はね・はらいの終端表現（可変太さストローク）
- 複数漢字対応
- centerLines → index.html アニメーションへの自動変換

---

## 2026-02-28（セッション1）

### やったこと
- プロジェクト初期整備
  - CLAUDE.md 作成（技術知見・実験経緯・今後の方針を記録）
  - .gitignore 作成（.wrangler/ と一時ファイルを除外）
  - index.html の「山」ストローク座標を微調整（stroke-width 10→12）
  - リモートバッチファイル作成（remote_bat_Kanji_Stroke.bat）

### 現状
- 「山」のアニメーションは動作する
- SVGパス座標の精度が課題（フォントオーバーレイとのズレが大きい）
- Cloudflare Pages にデプロイ済み: https://kanji-stroke-a8h.pages.dev

---

## 2026-02-27（初期実装）

### やったこと
- アニメーションエンジン実装（CSS transition + stroke-dashoffset）
- Noto Serif JP フォントオーバーレイ実装
- UI実装（スタート / リセット / 参照ON-OFF / 色分けトグル）
- Cloudflare Pages デプロイ

### 技術選定の経緯
- SVG SMIL animate → モバイル非対応で不採用
- JS + CSS transition → 全環境で安定動作、採用
